          <article class="sidebar">
            <h1 class="sidebar-title">Photographs on display</h1>
            <p class="sidebar-body">Synchronicity taurus the wisdom of your body, tahini heart-opening transformative.</p>
            <div class="pixgrid clearfix">
              <% if (photos.length > 0) { %>
              <% for (i=0; i< photos.length; i++) { %>
                    <img src="/images/photos/<%= photos[i] %>" alt="photos <%= i %>">
              <% } %>
              <% } %>
            </div>
          </article>